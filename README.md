# Python based satellite tracker

Finds satellite data using celestrak

To use it donwload the files, make sure they are in the same folder,then open the folder in your code  editor, make sure you are in the folder and type the following commaind the the terminal:
  python start.py

then it will start enter the name of the satellite/NORAD Id which you wish to track.
